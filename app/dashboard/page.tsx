import Link from 'next/link';
import Shell from '@/components/Shell';
import { LOT3 } from '@/lib/lot3';
import { LOT4 } from '@/lib/lot4';
import { MASTER_SCHEMES } from '@/lib/master';
const money=(n:number)=>'₹'+Math.round(n).toLocaleString('en-IN');
const l3=LOT3.reduce((s,r)=>s+r.approvedQty*r.rate,0),l4=LOT4.reduce((s,r)=>s+r.approvedQty*r.rate,0),master=MASTER_SCHEMES.reduce((s,r)=>s+r.estimatedValue,0),schemes=new Set([...LOT3,...LOT4].map(r=>r.schemeId||r.scheme)).size;
export default function Dashboard(){return <Shell><div className='content'><h1 className='title'>HQ Control Dashboard</h1><p className='muted'>Master Project: Sant Kabir Nagar Rural Water Supply Project</p><div className='grid grid4' style={{marginTop:20}}>{[['Master Estimate',money(master)],['Lot 3 Approved',money(l3)],['Lot 4 Approved',money(l4)],['Tracked Schemes',String(schemes)]].map(x=><div className='card' key={x[0]}><div className='muted'>{x[0]}</div><div className='kpi'>{x[1]}</div></div>)}</div><div className='grid grid3' style={{marginTop:16}}><div className='card'><h3>Project Manager</h3><p className='muted'>Enter execution quantities and measurements against approved BOQ.</p><Link className='btn secondary' href='/execution'>Open execution</Link></div><div className='card'><h3>Accountant</h3><p className='muted'>Control BOQ, reconciliation, RA bills, purchases, expenses and payments.</p><Link className='btn secondary' href='/boq'>Open BOQ control</Link></div><div className='card'><h3>HQ Inspector</h3><p className='muted'>Review lots, schemes, progress, value and exceptions.</p><Link className='btn secondary' href='/reports'>Open reports</Link></div></div><div className='card' style={{marginTop:16}}><h3>Working hierarchy</h3><p className='muted'>Project → Lot → Block → Scheme → Item → Approved Qty → Executed Qty → Measurement → RA Bill → Purchase → Expense → Payment.</p><Link className='btn' href='/schemes'>View schemes</Link></div></div></Shell>}
