'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import Link from 'next/link';
const roles=['MD','Accounts','Admin','Project Manager','Field Billing Engineer'];
const projects=['Sant Kabir Nagar','Siddharth Nagar','Mirzapur'];
export default function Login(){const [role,setRole]=useState('MD');const [project,setProject]=useState('Sant Kabir Nagar');const [show,setShow]=useState(false);const router=useRouter();return <main className="login"><section className="authcard"><div className="authbrand"><div className="hommark" aria-label="Hetvi O&M">H<span>O</span>M</div><div><div className="brandname">Hetvi O&amp;M</div><div className="companyname">Hetvi Operation and Maintenance Works</div></div></div><div className="authbody"><div><div className="eyebrow">SECURE ACCESS</div><h1>Welcome back</h1><p className="muted">Sign in to continue to Hetvi O&amp;M.</p></div><label className="field"><span>Role</span><select className="input" value={role} onChange={e=>setRole(e.target.value)}>{roles.map(x=><option key={x}>{x}</option>)}</select></label>{(role==='Project Manager'||role==='Field Billing Engineer')&&<label className="field"><span>Project</span><select className="input" value={project} onChange={e=>setProject(e.target.value)}>{projects.map(x=><option key={x}>{x}</option>)}</select></label>}<label className="field"><span>Email or User ID</span><input className="input" autoComplete="username" placeholder="Enter your email or user ID"/></label><label className="field"><span>Password</span><div className="password"><input className="input" type={show?'text':'password'} autoComplete="current-password" placeholder="Enter your password"/><button type="button" className="showpass" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></div></label><button className="btn authsubmit" onClick={()=>router.push('/dashboard')}>Sign in</button><div className="authlinks"><span>Forgot password?</span><Link href="/signup">Create an account</Link></div><p className="authnote">Accounts are approved and assigned access by Hetvi O&amp;M administration.</p></div></section></main>}
