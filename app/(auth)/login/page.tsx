'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
const roles=[['HQ Inspector','Portfolio oversight across divisions'],['Project Manager','Site/project execution and entries'],['Accountant','BOQ, bills, expenses and payments'],['Admin','Master data, users and controls'],['Field Engineer','Measurements, progress and site reports']];
export default function Login(){const [sel,setSel]=useState('HQ Inspector');const router=useRouter();return <main className="login"><section className="loginbox"><div className="loginhero"><div className="brand">Project<span>OS</span></div><h1 style={{fontSize:42,lineHeight:1.05}}>One operating system for the whole project.</h1><p style={{color:'#cbd5e1',fontSize:16,lineHeight:1.7}}>Control projects, lots, schemes, BOQs, execution, measurements, RA bills, expenses and profitability from one auditable system.</p><div style={{marginTop:40}}><div className="badge">Role-based access</div> <div className="badge">Lot-aware BOQ</div> <div className="badge">Audit ready</div></div></div><div className="loginform"><h2 style={{fontSize:30,marginBottom:4}}>Sign in</h2><p className="muted">Select your working role.</p><div className="rolegrid">{roles.map(([r,d])=><button key={r} className={'role '+(sel===r?'sel':'')} onClick={()=>setSel(r)}><strong>{r}</strong><small>{d}</small></button>)}</div><div className="form"><label>Email<input className="input" placeholder="name@company.com"/></label><label>Password<input className="input" type="password" placeholder="••••••••"/></label><button className="btn" onClick={()=>router.push('/dashboard')}>Continue as {sel}</button></div><p className="muted" style={{fontSize:12,marginTop:18}}>Production authentication will enforce permissions server-side; the role selector here is the initial UX.</p></div></section></main>}
