'use client';
import {useState} from 'react';
import Link from 'next/link';
const roles=['Project Manager','Field Billing Engineer','Accounts','Admin'];
const projects=['Sant Kabir Nagar','Siddharth Nagar','Mirzapur'];
export default function Signup(){const [role,setRole]=useState('Project Manager');const [project,setProject]=useState('Sant Kabir Nagar');const [show,setShow]=useState(false);return <main className="login"><section className="authcard"><div className="authbrand"><div className="hommark" aria-label="Hetvi O&M">H<span>O</span>M</div><div><div className="brandname">Hetvi O&amp;M</div><div className="companyname">Hetvi Operation and Maintenance Works</div></div></div><div className="authbody"><div><div className="eyebrow">NEW ACCOUNT</div><h1>Create your account</h1><p className="muted">Request access to the Hetvi O&amp;M system.</p></div><label className="field"><span>Full Name</span><input className="input" autoComplete="name" placeholder="Enter your full name"/></label><label className="field"><span>Email / User ID</span><input className="input" autoComplete="username" placeholder="Enter your email or preferred user ID"/></label><label className="field"><span>Account Type</span><select className="input" value={role} onChange={e=>setRole(e.target.value)}>{roles.map(x=><option key={x}>{x}</option>)}</select></label>{role==='Project Manager'&&<label className="field"><span>Project</span><select className="input" value={project} onChange={e=>setProject(e.target.value)}>{projects.map(x=><option key={x}>{x}</option>)}</select></label>}<label className="field"><span>Password</span><div className="password"><input className="input" type={show?'text':'password'} autoComplete="new-password" placeholder="Create a password"/><button type="button" className="showpass" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></div></label><label className="field"><span>Confirm Password</span><input className="input" type="password" autoComplete="new-password" placeholder="Confirm your password"/></label><button className="btn authsubmit" type="button">Request account</button><div className="authlinks"><span>Admin approval required</span><Link href="/login">Already have an account? Sign in</Link></div><p className="authnote">Your account will remain pending until Hetvi O&amp;M administration approves your role and project access.</p></div></section></main>}
